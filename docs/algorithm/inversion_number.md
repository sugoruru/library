---
title: 転倒数
documentation_of: ../../libs/algorithm/inversion_number.hpp
---

## 概要

配列の転倒数を求めるアルゴリズム.

## 使い方

- `inversion_number(a, compress)` := 配列 a の転倒数を返す. `compress` が true の場合、要素を圧縮して計算する.

## 計算量

- `inversion_number(a, compress)` : $O(n \log n)$
