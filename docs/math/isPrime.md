---
title: 素数判定(O(√N))
documentation_of: ../../libs/math/isPrime.hpp
---

## 概要

$N$ が素数かどうかを判定する.

## 使い方

- `isPrime(N)` := $N$ が素数かどうかを判定する.

## 計算量

- `isPrime(N)` : $O(\sqrt{N})$
