---
title: 素数列挙(O(n log log n))
documentation_of: ../../libs/math/primeList.hpp
---

## 概要

エラトステネスの篩を用いて、素数を列挙.

## 使い方

- `primeList(n)` := $n$ 以下の素数を列挙した配列を返す.

## 計算量

- `primeList(n)` : $O(n \log \log n)$
