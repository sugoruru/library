---
title: トポロジカルソート
documentation_of: ../../libs/graph/topological_sort.hpp
---

## 概要

有向グラフの頂点をトポロジカル順序でソートする.

## 使い方

- `topological_sort(G)` := グラフ G のトポロジカルソート結果を返す.

## 計算量

- `topological_sort(G)` : $O(V + E)$
